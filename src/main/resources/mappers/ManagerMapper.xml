<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.babsang.megabox.mappers.IManagerMapper">

    <select id="selectUserById"
            resultType="dev.babsang.megabox.entities.member.UserEntity">
        SELECT `email`             AS `email`,
               `name`              AS `name`,
               `birthday`          AS `birthday`,
               `contact`           AS `contact`,
               `id`                AS `id`,
               `password`          AS `password`,
               `address_postal`    AS `addressPostal`,
               `address_primary`   AS `addressPrimary`,
               `address_secondary` AS `addressSecondary`,
               `registered_on`     AS `registeredOn`,
               `admin_flag`        AS `adminFlag`
        FROM `mg_member`.`users`
        ORDER BY `registered_on` DESC
    </select>

    <select id="selectUserCount"
            resultType="int">
        SELECT COUNT(0)
        FROM `mg_member`.`users`
        <if test="criterion != null and criterion.equals('name')">
            AND REPLACE(`name`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
        <if test="criterion != null and criterion.equals('all')">
            AND REPLACE(`name`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
            OR REPLACE(`id`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
        <if test="criterion != null and criterion.equals('id')">
            AND REPLACE(`id`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
    </select>

    <select id="selectUserIdCount"
            resultType="dev.babsang.megabox.entities.member.UserEntity">
        SELECT
        `email` AS `email`,
        `name` AS `name`,
        `birthday` AS `birthday`,
        `contact` AS `contact`,
        `id` AS `id`,
        `password` AS `password`,
        `address_postal` AS `addressPostal`,
        `address_primary` AS `addressPrimary`,
        `address_secondary` AS `addressSecondary`,
        `registered_on` AS `registeredOn`,
        `admin_flag` AS `adminFlag`
        FROM `mg_member`.`users` AS `user`
        <if test="criterion != null and criterion.equals('name')">
            AND REPLACE(`user`.`name`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
        <if test="criterion != null and criterion.equals('all')">
            AND REPLACE(`user`.`name`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
            OR REPLACE(`user`.`id`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
        <if test="criterion != null and criterion.equals('id')">
            AND REPLACE(`id`, ' ', '') LIKE CONCAT('%', REPLACE(#{keyword}, ' ', ''), '%')
        </if>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <delete id="deleteUser">
        delete
        from mg_member.users
        where email = #{email}
    </delete>

    <select id="selectByEmail"
            resultType="dev.babsang.megabox.entities.member.UserEntity">
        SELECT `email`             AS `email`,
               `name`              AS `name`,
               `birthday`          AS `birthday`,
               `contact`           AS `contact`,
               `id`                AS `id`,
               `password`          AS `password`,
               `address_postal`    AS `addressPostal`,
               `address_primary`   AS `addressPrimary`,
               `address_secondary` AS `addressSecondary`,
               `registered_on`     AS `registeredOn`,
               `admin_flag`        AS `adminFlag`
        FROM `mg_member`.`users`
        WHERE BINARY `email` = #{email}
    </select>

</mapper>